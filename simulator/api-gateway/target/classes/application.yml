server:
  port: 8080

spring:
  application:
    name: api-gateway

order-service:
  url: ${ORDER_SERVICE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
